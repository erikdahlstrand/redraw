var chai = require('chai');
var expect = chai.expect;

describe('ArrowTool', function () {
	let canvasWrapper = {}, eventAggregator = {}
	let arrowTool = new ArrowTool(canvasWrapper, eventAggregator);
	
	it("should listen to events", function () {
		

	});
});